server:
  port: 8002

spring:
  application:
    name: auth-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: 674ea0ed-9be0-4c8b-88d9-1305aa71e336
        group: DEFAULT_GROUP
  datasource:
    url: jdbc:mysql://localhost:3306/power_trading_user?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: power_trading
    password: power_trading123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379
      password: redis123
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

jwt:
  secret: power-trading-secret-key-2024-this-is-a-very-long-secret-key-for-jwt-signing-must-be-at-least-32-characters-long
  expiration: 86400000

logging:
  level:
    com.power.trading: DEBUG
